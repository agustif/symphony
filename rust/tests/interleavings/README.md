# Interleaving Suite

Deterministic scheduler interleavings for orchestrator lifecycle events.

- Exhaustive order-preserving worker schedule generation for paired event streams.
- Per-schedule invariant checks and final-state assertions.
- Bounded state-size assertions to detect cross-issue contamination.
