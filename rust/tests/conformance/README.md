# Conformance Suite

Deterministic conformance coverage for:

- `symphony-domain` lifecycle reduction and invariants.
- release semantics across claimed/running/retry tracking.
- command emission cardinality across representative traces.
- workflow load/parse typed error behavior and prompt fallback semantics.
- config env/default precedence and invalid-field rejection behavior.
- tracker candidate/state fetch contract plus transport-error mapping behavior.
- app-server startup/turn ordering validation and stdout envelope parsing.
